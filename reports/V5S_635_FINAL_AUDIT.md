# Criterion 635 frozen interpretation

The V5S interpretation is explicit AST, ALT, BUN, and creatinine evidence with matching reference upper limits and all four observations at or below the permitted bound. Missing numeric values are insufficient for deterministic emission; no numeric defaults are fabricated. Semantic fallback may establish a positive only when the model receives grounded evidence, but the FHIR payload still requires grounded values.
